import { SplitLinkPipe } from './split-link.pipe';

describe('SplitLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
